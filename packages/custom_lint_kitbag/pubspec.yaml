name: custom_lint_kitbag
description: Architecture-enforcing lint rules for Kitbag.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  analyzer: ^8.4.0
  analyzer_plugin: ^0.13.0
  custom_lint_builder: ^0.8.1
