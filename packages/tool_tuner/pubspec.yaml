name: tool_tuner
description: Kitbag tuner tool — chromatic + instrument presets, auto string detection.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  go_router: ^16.0.0
  permission_handler: ^12.0.0
  core_audio_ffi:
  core_design:
  core_plugin_api:

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
