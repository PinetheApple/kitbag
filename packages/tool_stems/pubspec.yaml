name: tool_stems
description: Kitbag stem player — multi-track stem import, mixer, and A-B loop.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  go_router: ^16.0.0
  core_audio_ffi:
  core_db:
  core_design:
  core_plugin_api:
  core_services:
  file_picker: ^10.0.0
  path_provider: ^2.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
