name: core_db
description: Kitbag persistence — drift schema and DAOs for setlists and song presets.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.28.0
  drift_flutter: ^0.2.0

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.28.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
