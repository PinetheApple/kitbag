name: core_services
description: Concrete Riverpod provider definitions for Kitbag infrastructure services.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  core_audio_ffi:
  core_db:

dev_dependencies:
  flutter_lints: ^6.0.0
