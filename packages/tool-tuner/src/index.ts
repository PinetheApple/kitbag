// tool-tuner — plugin (SPEC §13.1). May import core-* only, never another
// tool, never app-shell.
//
// SKELETON (#27): empty. Tools are rebuilt in Phase 3 in dependency order
// (SPEC §15), on top of the proven 60fps foundation. Placeholder to anchor the
// package boundary.
export {};
