// core-design — tokens, theme, shared components: the waveform renderer, LEDs,
// steppers, sheets, numpad (SPEC §13.1). May import core-plugin-api only.
//
// SKELETON (#27): §12.2's tokens are the sole styling authority (§13.8.1) and
// the Tailwind theme is generated from the TS export — none of that is authored
// here yet (that pipeline is a later Phase 2 wave). Placeholder to anchor the
// package boundary.
export {};
