{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "agent": {
    "ralph": {
      "description": "Reviewer/critic for the feedback→refine loop. Use when another agent has completed work — a change to the C++ core, an architecture decision, a refactor — and needs a thorough review from a skeptical peer before presenting to the human.",
      "mode": "subagent",
      "permission": {
        "edit": "deny",
        "bash": {
          "cmake *": "allow",
          "./native/audio_core/build/*": "allow",
          "git *": "allow",
          "*": "ask"
        }
      }
    }
  }
}
